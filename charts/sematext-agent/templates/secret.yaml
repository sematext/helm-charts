{{- if not .Values.existingSecret.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "sematext-agent.fullname" . }}
  labels:
    app: {{ template "sematext-agent.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
type: Opaque
data:
  infra-token: {{ default "" .Values.infraToken | b64enc | quote }}
  logs-token: {{ default "" .Values.logsToken | b64enc | quote }}
  {{- range $appName, $appConfig := .Values.otel.apps }}
  {{- if index $appConfig "monitoring-token" }}
  otel-{{ $appName }}-monitoring-token: {{ index $appConfig "monitoring-token" | b64enc | quote }}
  {{- end }}
  {{- if index $appConfig "logs-token" }}
  otel-{{ $appName }}-logs-token: {{ index $appConfig "logs-token" | b64enc | quote }}
  {{- end }}
  {{- if index $appConfig "traces-token" }}
  otel-{{ $appName }}-traces-token: {{ index $appConfig "traces-token" | b64enc | quote }}
  {{- end }}
  {{- end }}
{{- end }}
